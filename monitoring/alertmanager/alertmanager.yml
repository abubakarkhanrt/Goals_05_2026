route:
  receiver: default
  group_by: [alertname, job]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: default
    # Demo stack: alerts visible at http://localhost:9090/alerts
    # Add email/webhook/Slack here for production.
